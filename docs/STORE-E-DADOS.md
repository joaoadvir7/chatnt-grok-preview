# Store e dados

Chave localStorage: `atendimento-nt-v17-name-fix`.
Arquivo: `src/lib/store.ts`.

Merge: `uniqueById` nas automações, `emptyWaba()`, diario/PIN.
Tipos: `src/lib/types.ts`.
CRM ainda não está no Postgres (PGLite/Neon só auth).
