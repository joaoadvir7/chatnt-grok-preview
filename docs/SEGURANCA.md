# Segurança

Tokens Meta só em `connection.waba.accessToken`. Nunca URL, README, commit, print.

Encerrar o dia: resumo + PIN SHA-256 `chatnt-pin:{pin}` + `sessionLocked`.
Não é vault. Limpar localStorage apaga PIN e dados.

Não usar WhatsApp não oficial. Não enviar marketing fora da janela 24h sem template.
